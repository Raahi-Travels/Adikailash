# Scene images confirmed as real photography.
#
# One filename per line, relative to public/scenes/. A file listed here is asserted
# to be an actual photograph taken by the team, with consent where people appear.
#
# `bun run check:imagery` fails while any file in public/scenes/ is missing from this
# list, which is what stops the AI placeholders from reaching the public site.
#
# Do not add a filename until the real photograph is in place. The list is the
# record; adding a line for an image that is still generated defeats the check.
